
-- ============================================
-- USEFUL QUERIES FOR REFERENCE
-- ============================================

-- Query 1: Get all events with their organizer and speaker count
-- SELECT 
--     e.id, e.title, e.event_date, e.capacity, e.registered_count, 
--     u.first_name, u.last_name,
--     COUNT(DISTINCT es.speaker_id) as speaker_count
-- FROM events e
-- JOIN users u ON e.created_by = u.id
-- LEFT JOIN event_speakers es ON e.id = es.event_id
-- GROUP BY e.id, e.title, e.event_date, e.capacity, e.registered_count, u.first_name, u.last_name
-- ORDER BY e.event_date;

-- Query 2: Get event registrations with user details
-- SELECT 
--     e.title, e.event_date,
--     u.first_name, u.last_name, u.email,
--     er.status, er.registered_at
-- FROM event_registrations er
-- JOIN events e ON er.event_id = e.id
-- JOIN users u ON er.user_id = u.id
-- ORDER BY e.event_date, e.title;

-- Query 3: Get attendance rate for each event
-- SELECT 
--     e.title, e.event_date,
--     COUNT(DISTINCT er.user_id) as total_registered,
--     COUNT(DISTINCT ea.user_id) as total_attended,
--     ROUND(100 * COUNT(DISTINCT ea.user_id) / COUNT(DISTINCT er.user_id), 2) as attendance_rate
-- FROM events e
-- LEFT JOIN event_registrations er ON e.id = er.event_id AND er.status != 'CANCELLED'
-- LEFT JOIN event_attendances ea ON e.id = ea.event_id
-- GROUP BY e.id, e.title, e.event_date;

-- Query 4: Get speakers for each event
-- SELECT 
--     e.title, e.event_date,
--     s.name, s.expertise, s.email
-- FROM events e
-- JOIN event_speakers es ON e.id = es.event_id
-- JOIN speakers s ON es.speaker_id = s.id
-- ORDER BY e.title, s.name;


password: tbbb unit yygw rezj
